# Copy this file to a new file named ".env" (in the same folder) and fill
# in your real API key. NEVER commit your real .env file to Git — only
# .env.example (with no real secrets) should be shared or version
# controlled.

# Get a free Gemini API key at https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here
